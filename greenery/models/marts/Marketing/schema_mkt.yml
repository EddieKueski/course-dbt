version: 2


models:
  - name: dim_affected_users
    description: Contains user's who got the orders late
    columns:
      - name: user_id
        tests: 
        - not_null
        - unique:
            config:
                store_failures: true  # always store failures



