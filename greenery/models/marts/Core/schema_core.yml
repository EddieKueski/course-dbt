version: 2


models:
  - name: dim_users
    description: Contains user's information 
    columns:
      - name: user_id
        tests: 
        - not_null
        - unique:
            config:
                store_failures: true  # always store failures
